# Admin app environment example
NEXTAUTH_URL=http://localhost:3002
NEXTAUTH_SECRET=replace-with-a-strong-random-string

# Google OAuth for Admin app (add http://localhost:3002/api/auth/callback/google in Google console)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# IMPORTANT: Point to the SAME database as frontend so users/sessions are shared.
# For SQLite, this path is relative to the admin folder and points at the frontend DB file.
DATABASE_URL="file:../frontend/prisma/dev.db"
